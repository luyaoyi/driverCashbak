# 会员权益膨胀后台改动说明

## 1. 原型地址

- 本地原型：`backend-prototype/membership-expansion.html`

## 2. 改动范围

本次在现有会员权益膨胀活动配置页中进行调整，保留基础信息、权益获取方式、参与限制、权益有效期配置和权益发放通知等现有能力，仅新增以下配置：

1. 活动配置模块新增“常规膨胀比例”。
2. 新增“前端样式”模块，支持配置“奖励中心入口文案”。

## 3. 活动配置调整

现有“限时膨胀比例”保持不变，在同一模块下新增“常规膨胀比例”。

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
| --- | --- | --- | --- | --- | --- |
| 常规膨胀比例 | 正整数 | 数字输入框 | 是 | 1% | 沿用现有限时膨胀比例的页面控件和取值范围，支持配置 1～50；历史活动未配置该字段时按 1% 回显。 |

页面要求：

- 常规膨胀比例与限时膨胀比例同时展示，分别配置。
- 输入框右侧展示单位“%”。
- 仅允许输入 1～50 的正整数，不支持零、负数和小数。
- 查看模式下字段只读，不允许修改。

## 4. 前端样式配置

活动配置页新增“前端样式”模块。

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
| --- | --- | --- | --- | --- | --- |
| 奖励中心入口文案 | 字符串 | 单行文本输入框 | 否 | 无 | 用于奖励中心展示会员权益膨胀入口文案；未填写时不展示自定义入口文案。历史活动默认为空。 |

## 5. 保存校验

- 常规膨胀比例未填写或不在 1～50 的正整数范围内时，不允许保存，并提示“常规膨胀比例需为1～50的正整数”。
- 奖励中心入口文案为非必填，未填写时不影响保存。
- 保存成功后，新配置与活动其他配置一并生效。

## 6. 不在本次调整范围

- 不调整现有限时膨胀比例的配置规则。
- 不调整权益获取、参与资格、权益有效期及通知方式的现有逻辑。
- 不新增奖励中心入口文案之外的前端样式字段。
