# 真车主完单返现后台配置说明

## 1. 原型地址

[真车主完单返现后台原型](https://luyaoyi.github.io/driverCashbak/)

## 2. 改动范围

在现有真车主完单返现活动编辑页中新增“任务中心聚合配置”模块，用于控制完单返现活动是否在任务中心展示，并配置任务中心任务卡片所需的展示内容。

现有活动基础信息、参与限制、任务配置、奖励配置、消息推送及原完单返现前端样式不在本次调整范围内。

## 3. 配置项说明

### 3.1 任务中心聚合开关

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
|---|---|---|---|---|---|
| 是否聚合到任务中心 | 枚举 | 单选框 | 是 | 否 | 选择“是”时，当前活动可在任务中心展示，并显示任务中心样式配置；选择“否”时，隐藏任务中心样式配置，活动继续沿用原完单返现展示逻辑。打开历史活动表单时，该字段选中“否”。 |

### 3.2 任务中心样式配置

仅当“是否聚合到任务中心”选择“是”时展示并生效。

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
|---|---|---|---|---|---|
| 任务标题 | 字符串 | 单行文本输入框 | 是 | 无 | 最多 40 个字符；未填写时不允许保存。 |
| 任务 icon | 图片 | 图片上传 | 否 | 无 | 仅允许上传 1 张图片，建议尺寸为 120×120，格式为 JPG 或 PNG。 |
| 任务标签 | 枚举 | 单选框 | 否 | 无 | 可选择“无”或“新手任务”；选择“新手任务”时展示新手任务专属配置。 |
| 新手任务 icon | 图片 | 图片上传 | 否 | 无 | 仅在任务标签选择“新手任务”时展示；仅允许上传 1 张图片，建议尺寸为 120×120，格式为 JPG 或 PNG。 |
| 新手任务标题 | 字符串 | 单行文本输入框 | 否 | 无 | 仅在任务标签选择“新手任务”时展示，最多 40 个字符。 |
| 新手任务副标题 | 字符串 | 单行文本输入框 | 否 | 无 | 仅在任务标签选择“新手任务”时展示，最多 60 个字符。 |
| 展示顺序 | 正整数 | 数字输入框 | 是 | 10 | 取值范围为 1～99，数值越小，在同类任务中的展示顺序越靠前。 |
| 任务标签 icon | 图片 | 图片上传 | 否 | 无 | 仅允许上传 1 张图片，建议尺寸为 120×120，格式为 JPG 或 PNG。 |
| 首页按钮引导标签 | 字符串 | 单行文本输入框 | 否 | 无 | 用于司机端首页任务入口引导文案，最多 6 个字符。 |
| 任务副标题 | 字符串 | 单行文本输入框 | 否 | 无 | 最多 60 个字符。 |
| 奖励名称 | 字符串 | 单行文本输入框 | 否 | 无 | 最多 200 个字符。 |
| 任务说明 | 字符串 | 多行文本输入框 | 否 | 无 | 最多 1500 个字符。 |
| 是否开启认证通过倒计时 | 枚举 | 单选框 | 是 | 关闭 | 用于前端呈现“X天内完单奖励现金”。选择“开启”时，展示“认证通过后天数”；选择“关闭”时隐藏该字段。历史活动默认关闭。 |
| 认证通过后天数 | 正整数 | 数字输入框 | 条件必填 | 无 | 仅在开启认证通过倒计时时展示并必填，支持配置 1～20。1 天按 24 小时计算；如配置 3 天，表示认证通过后 72 小时内展示倒计时。 |
| 奖励中心入口文案 | 字符串 | 单行文本输入框 | 否 | 无 | 用于奖励中心入口展示；未填写时不展示该文案。 |

## 4. 页面交互

- 新建活动页面首次进入时，“是否聚合到任务中心”默认选择“否”，任务中心样式配置隐藏。
- 打开历史活动表单时，“是否聚合到任务中心”选择“否”，任务中心样式配置隐藏。
- 选择“是”后，展开任务中心样式配置。
- 选择“否”后，隐藏任务中心样式配置，保存时不校验该区域字段。
- 任务标签选择“新手任务”后，展示新手任务 icon、标题和副标题；选择“无”时隐藏上述字段。
- 点击“任务标签 icon”上传框，可选择并上传 1 张任务标签图片；未上传时不展示任务标签 icon。
- 选择“开启”认证通过倒计时后，展示“认证通过后天数”；选择“关闭”后，隐藏并清空已填写的天数。
- 打开历史活动表单时，“是否开启认证通过倒计时”默认选择“关闭”，“奖励中心入口文案”默认为空。

## 5. 保存校验

- “是否聚合到任务中心”选择“是”时，任务标题必须填写。
- “是否聚合到任务中心”选择“是”时，展示顺序必须为 1～99 的正整数。
- 开启认证通过倒计时时，“认证通过后天数”必须填写 1～20 的正整数。
- 奖励中心入口文案为非必填，未填写时不影响保存。
- 任务标签 icon 为非必填；上传时仅支持单张 JPG 或 PNG 图片。
- 任一必填项或格式校验不通过时，不允许保存，并提示用户完成对应配置。
- 校验通过后，保存活动及任务中心聚合配置。

## 6. 生效规则

- 配置为“是”的有效活动，才可进入任务中心的完单返现任务查询范围。
- 配置为“否”的活动不在任务中心展示，原完单返现活动入口及活动执行逻辑不受影响。
- 本配置仅影响任务中心的查询和展示，不改变完单返现活动的参与资格、订单进度计算、奖励发放及打款逻辑。
