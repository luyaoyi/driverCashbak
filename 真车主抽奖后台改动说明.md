# 真车主抽奖后台改动说明

## 1. 原型地址

- 真车主抽奖后台原型：[https://luyaoyi.github.io/driverCashbak/car-owners-lottery.html](https://luyaoyi.github.io/driverCashbak/car-owners-lottery.html)

## 2. 改动范围

本次在现有真车主抽奖活动配置页中调整“前端样式配置”模块。基础信息、活动限制、抽奖次数、用户分组、真实奖品、外显奖品及消息触达等现有能力保持不变。

## 3. 前端样式配置调整

现有落地页头图、首页头图、首页公告栏、首页弹窗、订单详情页弹窗、活动说明和规则说明保持不变，新增以下字段：

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
| --- | --- | --- | --- | --- | --- |
| 奖励中心入口文案 | 字符串 | 单行文本输入框 | 否 | 无 | 用于奖励中心展示真车主抽奖入口文案；未填写时不展示自定义入口文案。历史活动默认为空。 |
| 最高奖励价值 | 正整数 | 数字输入框 | 否 | 无 | 单位为元；填写时仅支持正整数。仅用于前端展示，不参与抽奖及奖励发放计算。历史活动默认为空。 |

页面要求：

- 字段展示在“前端样式配置”模块内。
- 新建活动时默认为空，配置人员可按需填写。
- 编辑历史活动时，未保存过该字段的活动按空值回显。
- 查看模式下字段只读，不允许修改。

## 4. 保存与生效

- 奖励中心入口文案为非必填，未填写时不影响活动保存。
- 最高奖励价值为非必填；填写时仅支持正整数，校验不通过时不允许保存。
- 填写后与其他前端样式配置一并保存。
- 活动配置生效后，前端可按配置展示奖励中心入口文案和最高奖励价值。

## 5. 不在本次调整范围

- 不调整现有前端图片、活动说明和规则说明的配置及校验规则。
- 不调整抽奖资格、抽奖次数、用户分组、中奖概率和奖品发放逻辑。
- 最高奖励价值仅用于前端展示，不作为奖品实际价值或奖励发放金额的计算依据。
- 不调整外显奖品和消息触达配置。
