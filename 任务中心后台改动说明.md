# 真车主任务中心后台改动说明

## 1. 原型地址

- 任务中心后台：[https://luyaoyi.github.io/driverCashbak/task-center.html](https://luyaoyi.github.io/driverCashbak/task-center.html)

## 2. 改动范围

本次在现有真车主任务中心后台的基础上调整，保留任务基础配置、投放限制、任务配置、前端样式配置等现有能力，新增以下内容：

1. 根据任务类型限制可选的奖励类型。
2. 完单任务新增消息触达配置。

## 3. 奖励配置调整

| 任务类型 | 可选奖励 | 不可选奖励 | 页面处理 |
| --- | --- | --- | --- |
| 完单任务 | 普通奖励 | 现金奖励 | 选择完单任务后，奖励类型自动切换为普通奖励，现金奖励置灰不可选 |
| 发布行程任务 | 现金奖励 | 普通奖励 | 选择发布行程任务后，奖励类型自动切换为现金奖励，普通奖励置灰不可选 |
| 添加常用线路任务 | 现金奖励 | 普通奖励 | 选择添加常用线路任务后，奖励类型自动切换为现金奖励，普通奖励置灰不可选 |

配置要求：

- 切换任务类型时，奖励类型的可选范围需立即更新。
- 已选奖励类型与新任务类型不匹配时，页面自动切换到该任务类型允许的奖励类型。
- 不允许选择的奖励类型保留展示，但需置灰且不可操作。
- 保存时再次校验任务类型与奖励类型是否匹配。

## 4. 消息触达配置

消息触达仅用于完单任务，配置人员可决定该任务是否通过消息平台触达司机。

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置说明 |
| --- | --- | --- | --- | --- | --- |
| 消息触达方式 | 多选项 | 复选框组 | 否 | 不勾选 | 本次仅支持选择“马达平台” |

页面交互：

- 选择完单任务时，展示消息触达模块。
- 消息触达方式为非必填，可多选；本次仅提供“马达平台”一个选项。
- 历史配置进入编辑页时，消息触达方式默认不勾选。
- 未勾选任何消息触达方式时，表示该任务不配置消息触达，不影响保存。
- 选择发布行程任务或添加常用线路任务时，不展示消息触达模块。

## 5. 保存校验

- 完单任务只能保存普通奖励，并按现有规则校验普通奖励内容。
- 发布行程任务和添加常用线路任务只能保存现金奖励，并按现有规则校验现金奖励金额及奖励编码。
- 现金奖励金额需为 1～100 的整数。
- 消息触达方式不必填，未勾选时不阻塞保存。
- 当页面存在任务类型与奖励类型不匹配的情况时，不允许保存，并提示配置人员调整。

## 6. 不在本次调整范围

- 不调整任务基础配置、投放限制、任务进度和前端样式配置。
- 不新增马达平台之外的消息触达方式。
- 不改造现有完单返现后台，原后台继续保留并独立运行。
